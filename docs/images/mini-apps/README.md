# Mini-app renders

These AppKit renders use sample data. Native materials use the opaque bitmap fallback; live glass requires a WindowServer backdrop. See ../../WIDGETS.md for implemented capabilities, setup, and live validation limits.
